{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

<<<<<<< HEAD
{% block stylesheets %}
    {{ parent() }}  
    {% if app.request.attributes.get('_route') == 'app_register' %}
        <link rel="stylesheet" href="{{ asset('assets/css/Login.css') }}">
    {% endif %}
{% endblock %}

{% block body %}
<div class="flex items-center justify-center min-h-screen bg-gray-100">
    <div class="w-full max-w-md bg-white shadow-lg rounded-lg p-8">
        <div class="text-center mb-6">
            <h1 class="text-2xl font-semibold text-gray-800">S'inscrire</h1>
        </div>

        {{ form_start(registrationForm, { 'attr': { 'class': 'space-y-4' } }) }}

        <div>
            <label class="block text-gray-700">Nom d'utilisateur</label>
            {{ form_widget(registrationForm.username, { 'attr': { 'class': 'w-full px-4 py-2 border rounded-lg focus:ring focus:ring-red-300' } }) }}
        </div>

        <div>
            <label class="block text-gray-700">Email</label>
            {{ form_widget(registrationForm.email, { 'attr': { 'class': 'w-full px-4 py-2 border rounded-lg focus:ring focus:ring-red-300' } }) }}
        </div>

        <div>
            <label class="block text-gray-700">Mot de passe</label>
            {{ form_widget(registrationForm.plainPassword.first, { 'attr': { 'class': 'w-full px-4 py-2 border rounded-lg focus:ring focus:ring-red-300' } }) }}
        </div>

        <div>
            <label class="block text-gray-700">Confirmer le mot de passe</label>
            {{ form_widget(registrationForm.plainPassword.second, { 'attr': { 'class': 'w-full px-4 py-2 border rounded-lg focus:ring focus:ring-red-300' } }) }}
        </div>

        <div class="text-center mt-4">
            <button type="submit" class="w-full bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg transition duration-300">
                S'inscrire
            </button>
        </div>

        {{ form_end(registrationForm) }}

        <hr class="my-4">
        
        <div class="text-center">
            <a class="text-sm text-red-500 hover:underline" href="{{ path('app_login') }}">
                Se connecter
            </a>
        </div>
    </div>
</div>
=======
{% block body %}
    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword.first) }}
        {{ form_row(registrationForm.plainPassword.second) }}
        <button type="submit">S'inscrire</button>
    {{ form_end(registrationForm) }}
>>>>>>> main
{% endblock %}
